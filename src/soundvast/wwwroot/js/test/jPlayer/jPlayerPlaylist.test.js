import expect from "expect";

describe("Component: Test", () => {
    it("does stuff", () => {
        expect(true).toEqual(true);
    });
});
// debugger
// describe("<JPlayerPlaylist />", () => {
//     debugger
//     it("should render one <JPlayer /> component", () => {
//         debugger
//         const wrapper = shallow(<JPlayer />);
//         expect(wrapper.find(JPlayer)).to.have.length(1);
//     });
// });